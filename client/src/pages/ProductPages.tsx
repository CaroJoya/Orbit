import { useState, type ReactNode } from "react";
import {
  ArrowRight,
  Bell,
  BookOpen,
  CalendarDays,
  Check,
  CheckCircle2,
  ChevronDown,
  ChevronRight,
  CircleHelp,
  Compass,
  Copy,
  Download,
  ExternalLink,
  FileText,
  Heart,
  Home as HomeIcon,
  Layers3,
  LifeBuoy,
  LockKeyhole,
  MapPin,
  MessageCircle,
  MoreHorizontal,
  Navigation,
  Plus,
  Search,
  Settings2,
  ShieldCheck,
  Sparkles,
  Star,
  TicketCheck,
  TrendingUp,
  UserRound,
  UsersRound,
  WalletCards,
} from "lucide-react";
import { toast } from "sonner";
import { useLocation } from "wouter";

import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { cn } from "@/lib/utils";

const imageReference = "https://images.unsplash.com/photo-1469474968028-56623f02e42e?auto=format&fit=crop&w=1600&q=85";
const mapImage = "https://images.unsplash.com/photo-1454496522488-7a8e488e8606?auto=format&fit=crop&w=1600&q=85";
const stopImage = "https://images.unsplash.com/photo-1601050690597-df0568f70950?auto=format&fit=crop&w=1200&q=85";

const utilityNav = [
  { label: "Trips", path: "/trips", icon: Navigation },
  { label: "Explore", path: "/explore", icon: Compass },
  { label: "Field guide", path: "/guide", icon: BookOpen },
];

type UtilityRoute = "trips" | "explore" | "guide" | "activity" | "account";

function MiniLabel({ children, tone = "default" }: { children: ReactNode; tone?: "default" | "teal" | "amber" | "saffron" | "green" }) {
  return <span className={cn("eyebrow", tone === "teal" && "text-teal", tone === "amber" && "text-amber-dark", tone === "saffron" && "text-saffron", tone === "green" && "text-moss")}>{children}</span>;
}

function StatusChip({ children, tone = "neutral" }: { children: ReactNode; tone?: "neutral" | "green" | "amber" | "teal" }) {
  return <span className={cn("status-chip", `status-${tone}`)}><span className="status-dot" />{children}</span>;
}

function Brand({ onNavigate, inverse = false }: { onNavigate: (path: string) => void; inverse?: boolean }) {
  return <button type="button" onClick={() => onNavigate("/")} className="flex items-center gap-3 text-left" aria-label="Open Orbit home"><span className="brand-mark"><span className="brand-glyph">O</span></span><span className={cn("font-display text-[22px] font-semibold tracking-tighter", inverse ? "text-paper" : "text-ink")}>Orbit<span className={inverse ? "font-sans text-teal-light" : "font-sans text-teal"}>Sync</span><small className={cn("ml-2 align-middle font-sans text-[9px] font-bold uppercase tracking-[0.18em]", inverse ? "text-paper/40" : "text-ink-muted")}>Travel OS</small></span></button>;
}

function MobileDock({ active, onNavigate }: { active: UtilityRoute; onNavigate: (path: string) => void }) {
  const items = [
    { label: "Home", path: "/", icon: HomeIcon, key: "home" },
    { label: "Trips", path: "/trips", icon: Navigation, key: "trips" },
    { label: "Explore", path: "/explore", icon: Compass, key: "explore" },
    { label: "Guide", path: "/guide", icon: BookOpen, key: "guide" },
  ];
  return <nav className="mobile-dock lg:hidden" aria-label="Mobile navigation">{items.map((item) => { const Icon = item.icon; const selected = active === item.key; return <button key={item.label} type="button" onClick={() => onNavigate(item.path)} className={cn("mobile-dock-item", selected && "mobile-dock-item-active")}><Icon size={17} /><span>{item.label}</span></button>; })}</nav>;
}

function UtilityShell({ active, eyebrow, title, description, children }: { active: UtilityRoute; eyebrow: string; title: ReactNode; description: string; children: ReactNode }) {
  const [, setLocation] = useLocation();
  const navigate = (path: string) => setLocation(path);
  return <div className="min-h-screen bg-paper text-ink"><aside className="fixed inset-y-0 left-0 z-40 hidden w-63 flex-col bg-ink px-5 py-6 text-paper lg:flex"><Brand onNavigate={navigate} inverse /><div className="mt-14 px-2"><MiniLabel>Orbit workspace</MiniLabel><p className="mt-3 text-sm leading-6 text-paper/64">A calmer way to plan, operate, and share the road.</p></div><nav className="mt-8 space-y-1.5" aria-label="Workspace navigation">{utilityNav.map((item) => { const Icon = item.icon; const selected = active === item.label.toLowerCase().replace(" ", "-") || (active === "guide" && item.label === "Field guide"); return <button key={item.path} type="button" onClick={() => navigate(item.path)} className={cn("nav-item", selected && "nav-item-active")}><span className={cn("flex h-5.75 w-5.75 items-center justify-center rounded-lg", selected ? "bg-teal/20 text-teal-light" : "border border-paper/12 text-paper/40")}><Icon size={13} /></span><span className="flex-1 text-left">{item.label}</span>{selected && <span className="h-1.5 w-1.5 rounded-full bg-teal-light" />}</button>; })}</nav><div className="mt-8 border-t border-paper/10 pt-7"><p className="eyebrow px-2 text-paper/38">Your account</p><button type="button" onClick={() => navigate("/activity")} className={cn("workspace-link", active === "activity" && "workspace-link-active")}><Bell size={15} />Activity center<span className="ml-auto h-2 w-2 rounded-full bg-coral" /></button><button type="button" onClick={() => navigate("/account")} className={cn("workspace-link", active === "account" && "workspace-link-active")}><UserRound size={15} />Profile & settings</button></div><div className="mt-auto rounded-[20px] border border-paper/10 bg-paper/6 p-4"><div className="flex items-center justify-between"><MiniLabel>Story mode</MiniLabel><Sparkles size={15} className="text-saffron-light" /></div><p className="mt-3 text-sm font-semibold text-paper">Frontend only</p><p className="mt-1 text-xs leading-5 text-paper/48">All states are simulated for a clear product walkthrough.</p></div></aside><div className="lg:pl-63"><header className="sticky top-0 z-30 flex h-19 items-center justify-between border-b border-ink/10 bg-paper/90 px-5 backdrop-blur-xl sm:px-8 lg:px-9"><div className="lg:hidden"><Brand onNavigate={navigate} /></div><div className="hidden items-center gap-3 lg:flex"><span className="eyebrow text-teal">Workspace</span><span className="h-1 w-1 rounded-full bg-ink-muted/50" /><span className="text-sm font-medium text-ink-muted">{active === "activity" ? "Activity center" : active === "account" ? "Profile & settings" : "Designed for the whole trip"}</span></div><div className="flex items-center gap-2"><button type="button" onClick={() => navigate("/activity")} className="icon-button relative" aria-label="Open activity center"><Bell size={16} />{active !== "activity" && <span className="notification-dot" />}</button><button type="button" onClick={() => navigate("/account")} className="flex h-9 w-9 items-center justify-center rounded-full bg-ink text-xs font-bold text-paper transition-transform hover:scale-[1.03]" aria-label="Open profile">AS</button></div></header><main className="main-shell mx-auto max-w-375 px-5 py-8 pb-28 sm:px-8 lg:px-10 lg:py-10 lg:pb-10"><div className="mb-9 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><MiniLabel tone="teal">{eyebrow}</MiniLabel><h1 className="mt-3 max-w-4xl font-display text-5xl font-semibold leading-[0.96] tracking-[-0.07em] text-ink sm:text-6xl">{title}</h1><p className="mt-5 max-w-2xl text-[15px] leading-7 text-ink-muted">{description}</p></div><div className="hidden items-center gap-2 md:flex"><button type="button" onClick={() => navigate("/")} className="inline-flex h-10 items-center gap-2 rounded-xl border border-ink/12 bg-paper px-3 text-xs font-bold text-ink transition-colors hover:bg-paper-dark"><Sparkles size={14} className="text-teal" /> Open live demo</button></div></div>{children}</main><MobileDock active={active} onNavigate={navigate} /></div></div>;
}

function MetricTile({ icon: Icon, label, value, copy, tone }: { icon: typeof Navigation; label: string; value: string; copy: string; tone: "teal" | "green" | "saffron" | "amber" }) {
  return <Card className="metric-card"><div className={cn("metric-icon", `metric-${tone}`)}><Icon size={16} /></div><div><p className="text-[11px] font-bold uppercase tracking-[0.14em] text-ink-muted">{label}</p><p className="mt-1 font-display text-[26px] font-semibold leading-none tracking-tighter text-ink">{value}</p><p className="mt-2 text-xs text-ink-muted">{copy}</p></div></Card>;
}

function TripHero({ onNavigate }: { onNavigate: (path: string) => void }) {
  return <Card className="overflow-hidden p-0"><div className="grid lg:grid-cols-[1.1fr_0.9fr]"><div className="relative min-h-90 overflow-hidden bg-ink"><img src={imageReference} alt="Rajasthan road toward Jaipur" className="absolute inset-0 h-full w-full object-cover opacity-80" /><div className="absolute inset-0 bg-linear-to-t from-ink via-ink/35 to-transparent" /><div className="relative flex h-full flex-col justify-between p-6 text-paper sm:p-8"><div className="flex items-center justify-between"><StatusChip tone="green">Live trip</StatusChip><span className="rounded-full border border-paper/18 bg-ink/20 px-3 py-2 text-[10px] font-bold uppercase tracking-[0.14em] backdrop-blur-sm">TRP · 8029</span></div><div><p className="eyebrow text-paper/55">12 — 14 February 2026 · 3 days</p><h2 className="mt-3 max-w-lg font-display text-4xl font-semibold leading-none tracking-[-0.06em] sm:text-5xl">Jaipur, with room for <em className="text-saffron-light">wonder.</em></h2><div className="mt-6 flex flex-wrap gap-2"><span className="rounded-full bg-paper/12 px-3 py-2 text-xs font-semibold backdrop-blur-sm">8 waypoints</span><span className="rounded-full bg-paper/12 px-3 py-2 text-xs font-semibold backdrop-blur-sm">30m protected buffer</span><span className="rounded-full bg-paper/12 px-3 py-2 text-xs font-semibold backdrop-blur-sm">₹49,240 held</span></div></div></div></div><div className="flex flex-col justify-between p-6 sm:p-8"><div><div className="flex items-center justify-between"><div><MiniLabel tone="teal">Trip pulse</MiniLabel><p className="mt-2 text-sm font-bold">Day 1 of 3 · Route is holding</p></div><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-teal/10 text-teal"><TrendingUp size={20} /></div></div><div className="mt-7"><div className="flex items-center justify-between text-xs font-bold"><span className="text-ink-muted">Journey completion</span><span className="text-teal">38%</span></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-ink/8"><div className="h-full w-[38%] rounded-full bg-teal" /></div></div><div className="mt-7 space-y-3"><div className="flex items-center gap-3 rounded-xl bg-paper-dark p-3"><span className="flex h-8 w-8 items-center justify-center rounded-lg bg-teal/10 text-teal"><HomeIcon size={15} /></span><div><p className="text-xs font-bold">Home pickup</p><p className="mt-1 text-[11px] text-ink-muted">07:30 · Driver Rajesh K.</p></div><Check size={15} className="ml-auto text-moss" /></div><div className="flex items-center gap-3 rounded-xl bg-paper-dark p-3"><span className="flex h-8 w-8 items-center justify-center rounded-lg bg-saffron/12 text-saffron"><TicketCheck size={15} /></span><div><p className="text-xs font-bold">Local stop voucher</p><p className="mt-1 text-[11px] text-ink-muted">Sharma Ji · fixed ₹120</p></div><ChevronRight size={15} className="ml-auto text-ink-muted" /></div></div></div><div className="mt-8 flex flex-col gap-2 sm:flex-row"><Button onClick={() => onNavigate("/")} className="h-11 flex-1 rounded-xl bg-ink font-bold text-paper hover:bg-ink/90">Open live trip <ArrowRight size={15} className="ml-2" /></Button><button type="button" onClick={() => { navigator.clipboard?.writeText("Jaipur circuit · TRP-8029"); toast.success("Trip link copied"); }} className="inline-flex h-11 items-center justify-center gap-2 rounded-xl border border-ink/12 px-4 text-xs font-bold text-ink-muted transition-colors hover:bg-paper-dark"><Copy size={14} /> Share</button></div></div></div></Card>;
}

export function TripsPage() {
  const [, setLocation] = useLocation();
  return <UtilityShell active="trips" eyebrow="Your trip shelf" title={<>Keep the <em>good routes</em> close.</>} description="Every itinerary has a live pulse, a paper trail, and a clear way back into the journey. Nothing is buried in a stale confirmation email."><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><MetricTile icon={Navigation} label="Active trips" value="01" copy="Jaipur circuit · live" tone="teal" /><MetricTile icon={CalendarDays} label="Upcoming" value="02" copy="Drafts ready to shape" tone="saffron" /><MetricTile icon={WalletCards} label="Held value" value="₹49,240" copy="Transparent trip ledger" tone="green" /><MetricTile icon={Heart} label="Saved routes" value="07" copy="Your future good ideas" tone="amber" /></div><div className="mt-7 space-y-7"><TripHero onNavigate={setLocation} /><div className="flex items-center justify-between"><div><MiniLabel>All routes</MiniLabel><h2 className="mt-2 font-display text-2xl font-semibold tracking-tighter">The rest of your shelf</h2></div><button type="button" onClick={() => setLocation("/explore")} className="inline-flex items-center gap-2 text-xs font-bold text-teal hover:underline">Find a new route <ArrowRight size={14} /></button></div><div className="grid gap-4 lg:grid-cols-3"><SavedTripCard image={mapImage} label="Draft route" title="Monsoon coast · Konkan" copy="4 days · food, ferries, slow mornings" meta="Last edited yesterday" tone="teal" onClick={() => toast.info("Draft opened in visual preview")} /><SavedTripCard image={stopImage} label="Saved idea" title="Pushkar after dark" copy="A two-night add-on for the next long weekend" meta="Saved 12 Jan 2026" tone="saffron" onClick={() => setLocation("/explore")} /><SavedTripCard image={imageReference} label="Past trip" title="Udaipur lakes loop" copy="Completed · 5 waypoints · 4.9 route score" meta="November 2025" tone="green" onClick={() => toast.success("Trip recap ready to explore")} /></div></div></UtilityShell>;
}

function SavedTripCard({ image, label, title, copy, meta, tone, onClick }: { image: string; label: string; title: string; copy: string; meta: string; tone: "teal" | "saffron" | "green"; onClick: () => void }) {
  return <button type="button" onClick={onClick} className="group overflow-hidden rounded-[20px] border border-ink/9 bg-paper text-left shadow-[0_10px_26px_rgba(23,34,35,0.04)] transition-all duration-200 hover:-translate-y-0.5 hover:shadow-[0_16px_32px_rgba(23,34,35,0.09)]"><div className="relative h-36 overflow-hidden"><img src={image} alt="" className="h-full w-full object-cover transition-transform duration-300 group-hover:scale-[1.03]" /><div className="absolute inset-0 bg-linear-to-t from-ink/70 to-transparent" /><span className="absolute bottom-3 left-4"><MiniLabel tone={tone}>{label}</MiniLabel></span><span className="absolute right-3 top-3 flex h-8 w-8 items-center justify-center rounded-lg bg-paper/88 text-ink backdrop-blur-sm"><ArrowRight size={14} /></span></div><div className="p-4"><h3 className="font-display text-xl font-semibold tracking-tighter">{title}</h3><p className="mt-2 text-xs leading-5 text-ink-muted">{copy}</p><p className="mt-4 text-[10px] font-bold uppercase tracking-[0.12em] text-ink-muted">{meta}</p></div></button>;
}

const exploreRoutes = [
  { tag: "Slow & local", title: "Rajasthan, off the obvious road", copy: "Courtyard stays, roadside chai, and enough empty space between the landmarks.", image: imageReference, meta: "3 days · ₹42k from Jaipur", tone: "saffron" as const, likes: "1.2k" },
  { tag: "Waterline", title: "Konkan by ferry and feeling", copy: "A coastal route with seafood kitchens, quiet beaches, and no forced early mornings.", image: stopImage, meta: "4 days · ₹55k from Mumbai", tone: "teal" as const, likes: "864" },
  { tag: "Rail & ridge", title: "Kumaon without the rush", copy: "Small train windows, forest walks, and a driver who knows when to pull over.", image: mapImage, meta: "5 days · ₹68k from Delhi", tone: "green" as const, likes: "2.1k" },
];

export function ExplorePage() {
  const [, setLocation] = useLocation();
  const [filter, setFilter] = useState("For you");
  const [saved, setSaved] = useState<string[]>([]);
  const filters = ["For you", "Slow travel", "Food first", "Scenic drives", "Weekend"];
  return <UtilityShell active="explore" eyebrow="Route library" title={<>Find the road that feels <em>like you.</em></>} description="Browse route ideas built around pace, appetite, and the kind of memory you want to bring home. Choose a starting shape; the system does the detailed bending later."><div className="flex flex-wrap items-center justify-between gap-4 border-y border-ink/9 py-4"><div className="flex flex-wrap gap-2">{filters.map((item) => <button key={item} type="button" onClick={() => setFilter(item)} className={cn("rounded-full border px-3 py-2 text-xs font-bold transition-colors", filter === item ? "border-teal/25 bg-teal/10 text-teal" : "border-ink/10 text-ink-muted hover:border-teal/25 hover:text-teal")}>{item}</button>)}</div><button type="button" onClick={() => toast.info("Search is a visual placeholder — try a mood filter instead")} className="inline-flex items-center gap-2 rounded-xl border border-ink/10 px-3 py-2 text-xs font-bold text-ink-muted hover:bg-paper-dark"><Search size={14} /> Search routes</button></div><div className="mt-7 grid gap-5 lg:grid-cols-3">{exploreRoutes.map((route) => { const isSaved = saved.includes(route.title); return <Card key={route.title} className="group overflow-hidden p-0"><div className="relative h-56 overflow-hidden"><img src={route.image} alt="" className="h-full w-full object-cover transition-transform duration-300 group-hover:scale-[1.03]" /><div className="absolute inset-0 bg-linear-to-t from-ink/70 via-transparent to-transparent" /><span className="absolute left-4 top-4 rounded-full bg-paper/88 px-3 py-2 text-[10px] font-bold uppercase tracking-[0.12em] text-ink backdrop-blur-sm">{route.tag}</span><button type="button" onClick={() => setSaved((current) => isSaved ? current.filter((item) => item !== route.title) : [...current, route.title])} className={cn("absolute right-4 top-4 flex h-9 w-9 items-center justify-center rounded-full backdrop-blur-sm transition-colors", isSaved ? "bg-saffron text-ink" : "bg-paper/88 text-ink-muted hover:text-saffron")} aria-label={isSaved ? `Remove ${route.title} from saved routes` : `Save ${route.title}`}><Heart size={15} fill={isSaved ? "currentColor" : "none"} /></button><div className="absolute bottom-4 left-4 flex items-center gap-1.5 text-xs font-bold text-paper"><Star size={13} fill="currentColor" className="text-saffron-light" /> {route.likes} route saves</div></div><div className="p-5"><h2 className="font-display text-2xl font-semibold leading-tight tracking-tighter">{route.title}</h2><p className="mt-3 text-sm leading-6 text-ink-muted">{route.copy}</p><div className="mt-5 flex items-center justify-between border-t border-ink/8 pt-4"><span className="text-[10px] font-bold uppercase tracking-[0.12em] text-ink-muted">{route.meta}</span><button type="button" onClick={() => setLocation("/")} className="inline-flex items-center gap-1 text-xs font-bold text-teal">Use this shape <ArrowRight size={13} /></button></div></div></Card>; })}</div><Card className="mt-6 border-teal/15 bg-teal/5 p-5 sm:p-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><div className="flex h-11 w-11 shrink-0 items-center justify-center rounded-2xl bg-teal text-white"><WandIcon /></div><div className="flex-1"><MiniLabel tone="teal">Not sure where to start?</MiniLabel><p className="mt-2 text-sm leading-6 text-ink/75">Tell us your mood, your hard stops, and your available time. The intake canvas turns a blank page into a route you can react to.</p></div><Button onClick={() => setLocation("/")} className="h-11 rounded-xl bg-ink font-bold text-paper hover:bg-ink/90">Open intake canvas <ArrowRight size={15} className="ml-2" /></Button></div></Card></UtilityShell>;
}

function WandIcon() { return <Sparkles size={19} />; }

const faqItems = [
  { question: "What happens when plans change mid-trip?", answer: "Orbit checks the full chain — timing, distance, preference fit, vendor availability, and cost — before offering a small set of safe alternatives. The visual demo shows this as the Ripple Engine." },
  { question: "How does the local vendor bridge protect privacy?", answer: "The partner sees a proxy ID and the exact fixed-rate order, not a traveler's personal number or full profile. Confirmations flow back into the trip pass through the operator workspace." },
  { question: "Is my itinerary ready without signal?", answer: "The digital pass is designed as the offline-ready surface: pickup details, vouchers, hotel timing, and masked driver contact are kept together for the day." },
  { question: "Can I keep control over the budget?", answer: "Yes. The intake canvas starts with a hard spending limit, while every route change shows a visible delta before anything is confirmed." },
];

export function GuidePage() {
  const [open, setOpen] = useState(0);
  const [, setLocation] = useLocation();
  return <UtilityShell active="guide" eyebrow="Field guide" title={<>The calm behind the <em>moving parts.</em></>} description="A short guide to how Orbit behaves when the road is beautiful, complicated, and occasionally rainy. Use it to read the visual story like a real product."><div className="grid gap-6 xl:grid-cols-[1fr_0.8fr]"><Card className="overflow-hidden p-0"><div className="relative h-64 bg-ink"><img src={mapImage} alt="Illustrated route map" className="h-full w-full object-cover opacity-80" /><div className="absolute inset-0 bg-linear-to-t from-ink via-ink/15 to-transparent" /><div className="absolute bottom-5 left-6 text-paper sm:left-8"><MiniLabel tone="teal">A visual product walkthrough</MiniLabel><h2 className="mt-2 max-w-xl font-display text-3xl font-semibold leading-tight tracking-tighter">From one preference to five connected surfaces.</h2></div></div><div className="grid gap-3 p-5 sm:grid-cols-3 sm:p-6"><GuideStep number="01" title="Shape" copy="The traveler sets the pace, budget, and non-negotiables." tone="teal" /><GuideStep number="02" title="Connect" copy="The route aligns stays, vendors, drivers, and buffers." tone="saffron" /><GuideStep number="03" title="Adapt" copy="One decision updates the whole story instead of one stale plan." tone="green" /></div></Card><Card className="p-6"><div className="flex items-start gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-saffron/14 text-saffron"><CircleHelp size={19} /></div><div><MiniLabel tone="saffron">Read this first</MiniLabel><h2 className="mt-2 font-display text-2xl font-semibold tracking-tighter">The product promise</h2></div></div><blockquote className="mt-6 border-l-2 border-teal/45 pl-4 text-base leading-7 text-ink/75">"Travel plans should be sturdy enough to change, and human enough to still feel like yours."</blockquote><div className="mt-6 flex items-center gap-2 text-xs font-bold text-teal"><ShieldCheck size={15} /> Built around visible trust</div><Button onClick={() => setLocation("/")} className="mt-6 h-11 w-full rounded-xl bg-ink font-bold text-paper hover:bg-ink/90">Walk through the demo <ArrowRight size={15} className="ml-2" /></Button></Card></div><div className="mt-7 grid gap-6 lg:grid-cols-[1fr_0.7fr]"><Card className="p-5 sm:p-6"><div className="flex items-center justify-between border-b border-ink/8 pb-5"><div><MiniLabel>Common questions</MiniLabel><h2 className="mt-2 font-display text-2xl font-semibold tracking-tighter">How it behaves in the wild</h2></div><BookOpen size={20} className="text-teal" /></div><div className="mt-2 divide-y divide-ink/8">{faqItems.map((item, index) => <div key={item.question} className="py-4"><button type="button" onClick={() => setOpen(open === index ? -1 : index)} className="flex w-full items-center justify-between gap-4 text-left"><span className="text-sm font-bold text-ink">{item.question}</span><ChevronDown size={16} className={cn("shrink-0 text-ink-muted transition-transform", open === index && "rotate-180 text-teal")} /></button>{open === index && <p className="mt-3 max-w-2xl text-sm leading-6 text-ink-muted animate-fade-up">{item.answer}</p>}</div>)}</div></Card><div className="space-y-5"><Card className="p-5"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-teal/10 text-teal"><LifeBuoy size={18} /></div><div><MiniLabel tone="teal">Need a human?</MiniLabel><p className="mt-1 text-sm font-bold">Talk to the trip desk</p></div></div><p className="mt-4 text-sm leading-6 text-ink-muted">A visual placeholder for support routing — no message is sent in this demo.</p><button type="button" onClick={() => toast.success("Trip desk handoff prepared")} className="mt-4 inline-flex items-center gap-2 text-xs font-bold text-teal">Open support handoff <ArrowRight size={13} /></button></Card><Card className="border-amber/18 bg-amber/7 p-5"><div className="flex items-center gap-2"><FileText size={16} className="text-amber-dark" /><MiniLabel tone="amber">Release note · v0.8</MiniLabel></div><p className="mt-3 text-sm leading-6 text-ink/72">The demo now includes the trip shelf, route library, activity center, and a field guide so every primary navigation choice has somewhere thoughtful to land.</p></Card></div></div></UtilityShell>;
}

function GuideStep({ number, title, copy, tone }: { number: string; title: string; copy: string; tone: "teal" | "saffron" | "green" }) {
  return <div className="rounded-2xl bg-paper-dark p-4"><span className={cn("text-[10px] font-bold uppercase tracking-[0.13em]", tone === "teal" ? "text-teal" : tone === "saffron" ? "text-saffron" : "text-moss")}>{number}</span><h3 className="mt-2 font-display text-xl font-semibold tracking-tighter">{title}</h3><p className="mt-2 text-xs leading-5 text-ink-muted">{copy}</p></div>;
}

export function ActivityPage() {
  const [, setLocation] = useLocation();
  const [allRead, setAllRead] = useState(false);
  const activity = [
    { icon: CloudIcon, tone: "amber", title: "Weather watch raised", copy: "Heavy rain may affect Aanya's outdoor trek on Day 2.", time: "8 min ago", unread: true },
    { icon: CheckCircle2, tone: "green", title: "Vendor confirmation received", copy: "Sharma Ji Samosa Hub confirmed the fixed-rate voucher.", time: "22 min ago", unread: true },
    { icon: MessageCircle, tone: "teal", title: "Driver thread updated", copy: "Rajesh is 12 minutes ahead and the pickup window is protected.", time: "1 hr ago", unread: false },
    { icon: Heart, tone: "saffron", title: "Route saved", copy: "Konkan by ferry and feeling was added to your shelf.", time: "Yesterday", unread: false },
  ];
  return <UtilityShell active="activity" eyebrow="Activity center" title={<>The trip keeps you <em>in the loop.</em></>} description="A single place for route changes, confirmations, and quiet system signals. Nothing urgent is disguised as a generic badge."><div className="flex flex-col justify-between gap-4 rounded-[20px] border border-teal/15 bg-teal/5 p-5 sm:flex-row sm:items-center sm:p-6"><div className="flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-teal text-white"><Bell size={19} /></div><div><MiniLabel tone="teal">Live signal layer</MiniLabel><p className="mt-2 text-sm font-bold">{allRead ? "You're all caught up." : "2 updates need your attention."}</p></div></div><button type="button" onClick={() => setAllRead(true)} className="text-left text-xs font-bold text-teal hover:underline sm:text-right">{allRead ? "Everything read" : "Mark all as read"}</button></div><div className="mt-7 grid gap-6 lg:grid-cols-[1fr_0.62fr]"><Card className="p-5 sm:p-6"><div className="flex items-center justify-between border-b border-ink/8 pb-5"><div><MiniLabel>Recent activity</MiniLabel><h2 className="mt-2 font-display text-2xl font-semibold tracking-tighter">Signals from the route</h2></div><button type="button" onClick={() => toast.info("Activity filters are ready for a connected backend")} className="icon-button" aria-label="Activity options"><MoreHorizontal size={17} /></button></div><div className="mt-2 divide-y divide-ink/8">{activity.map((item) => { const Icon = item.icon; const unread = item.unread && !allRead; return <button type="button" key={item.title} onClick={() => item.title.includes("Weather") ? setLocation("/") : toast.success("Activity detail opened")} className="flex w-full items-start gap-3 py-4 text-left transition-colors hover:bg-paper-dark/55"><span className={cn("flex h-9 w-9 shrink-0 items-center justify-center rounded-xl", item.tone === "amber" ? "bg-amber/35 text-amber-dark" : item.tone === "green" ? "bg-moss/10 text-moss" : item.tone === "saffron" ? "bg-saffron/14 text-saffron" : "bg-teal/10 text-teal")}><Icon size={15} /></span><span className="min-w-0 flex-1"><span className="flex items-center gap-2"><strong className="text-sm text-ink">{item.title}</strong>{unread && <span className="h-1.5 w-1.5 rounded-full bg-coral" />}</span><span className="mt-1 block text-xs leading-5 text-ink-muted">{item.copy}</span></span><span className="shrink-0 text-[10px] font-bold uppercase tracking-widest text-ink-muted">{item.time}</span></button>; })}</div></Card><div className="space-y-5"><Card className="p-5"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-moss/10 text-moss"><ShieldCheck size={18} /></div><div><MiniLabel tone="green">Notification hygiene</MiniLabel><p className="mt-1 text-sm font-bold">Only useful interruptions</p></div></div><p className="mt-4 text-sm leading-6 text-ink-muted">Trip-critical signals are separated from inspiration, receipts, and partner chatter so the next action stays obvious.</p><div className="mt-5 space-y-2 text-xs"><div className="flex items-center justify-between rounded-xl bg-paper-dark p-3"><span>Route risk</span><StatusChip tone="amber">1 open</StatusChip></div><div className="flex items-center justify-between rounded-xl bg-paper-dark p-3"><span>Vendor replies</span><StatusChip tone="green">3 clear</StatusChip></div></div></Card><Card className="border-ink/9 bg-ink p-5 text-paper"><MiniLabel tone="teal">Quick return</MiniLabel><p className="mt-2 font-display text-2xl font-semibold leading-tight tracking-tighter">Open the live trip where the signal began.</p><button type="button" onClick={() => setLocation("/")} className="mt-5 inline-flex items-center gap-2 text-xs font-bold text-teal-light">Go to Jaipur circuit <ArrowRight size={13} /></button></Card></div></div></UtilityShell>;
}

function CloudIcon() { return <span className="text-[17px]">☁</span>; }

export function AccountPage() {
  const [, setLocation] = useLocation();
  const [quiet, setQuiet] = useState(false);
  const [offline, setOffline] = useState(true);
  return <UtilityShell active="account" eyebrow="Profile & settings" title={<>Make the system fit <em>your way of travelling.</em></>} description="Small preferences, visible defaults, and the trust controls that make a shared trip feel personal without making it complicated."><div className="grid gap-6 xl:grid-cols-[0.85fr_1.15fr]"><Card className="p-6 sm:p-8"><div className="flex items-center gap-4"><div className="flex h-16 w-16 items-center justify-center rounded-[20px] bg-ink text-lg font-bold text-paper">AS</div><div><MiniLabel tone="teal">Traveler profile</MiniLabel><h2 className="mt-2 font-display text-3xl font-semibold tracking-[-0.06em]">Aanya Sharma</h2><p className="mt-1 text-sm text-ink-muted">Road-trip curious · prefers local layers</p></div></div><div className="mt-8 grid grid-cols-2 gap-3"><div className="rounded-2xl bg-paper-dark p-4"><p className="eyebrow">Trips shaped</p><p className="mt-2 font-display text-2xl font-semibold">12</p></div><div className="rounded-2xl bg-paper-dark p-4"><p className="eyebrow">Route score</p><p className="mt-2 font-display text-2xl font-semibold text-teal">4.9</p></div></div><div className="mt-6 flex items-center gap-2 rounded-xl border border-teal/15 bg-teal/5 p-3 text-xs leading-5 text-ink/72"><LockKeyhole size={15} className="shrink-0 text-teal" /> Your personal details stay separate from vendor-facing trip requests.</div></Card><Card className="p-6 sm:p-8"><div className="flex items-center justify-between border-b border-ink/8 pb-5"><div><MiniLabel>Controls</MiniLabel><h2 className="mt-2 font-display text-2xl font-semibold tracking-tighter">Quietly in control</h2></div><Settings2 size={20} className="text-teal" /></div><div className="mt-5 space-y-3"><SettingRow icon={Bell} title="Trip-critical updates" copy="Weather, pickup, vendor, and route changes" value="Always on" locked /><SettingRow icon={Sparkles} title="Inspiration nudges" copy="New route ideas based on your saved moods" value={quiet ? "Paused" : "On"} onClick={() => setQuiet(!quiet)} /><SettingRow icon={Download} title="Offline pass" copy="Keep today's route available without signal" value={offline ? "Ready" : "Off"} onClick={() => setOffline(!offline)} /></div><div className="mt-6 border-t border-ink/8 pt-5"><p className="eyebrow">Default travel mood</p><div className="mt-3 flex flex-wrap gap-2"><span className="style-chip style-chip-selected"><Compass size={14} /> Scenic drives <Check size={13} className="text-teal" /></span><span className="style-chip style-chip-selected"><Heart size={14} /> Local food <Check size={13} className="text-teal" /></span><button type="button" onClick={() => toast.info("Mood editor opens from the intake canvas")} className="style-chip"><Plus size={14} /> Add mood</button></div></div><Button onClick={() => setLocation("/")} className="mt-7 h-11 w-full rounded-xl bg-ink font-bold text-paper hover:bg-ink/90">Return to trip workspace <ArrowRight size={15} className="ml-2" /></Button></Card></div></UtilityShell>;
}

function SettingRow({ icon: Icon, title, copy, value, onClick, locked }: { icon: typeof Bell; title: string; copy: string; value: string; onClick?: () => void; locked?: boolean }) {
  return <div className="flex items-center gap-3 rounded-2xl bg-paper-dark p-3.5"><span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-paper text-teal"><Icon size={16} /></span><div className="min-w-0 flex-1"><p className="text-xs font-bold text-ink">{title}</p><p className="mt-1 truncate text-[11px] text-ink-muted">{copy}</p></div>{locked ? <span className="flex items-center gap-1 text-[10px] font-bold text-moss"><LockKeyhole size={12} /> {value}</span> : <button type="button" onClick={onClick} className={cn("rounded-lg px-2.5 py-2 text-[10px] font-bold transition-colors", value === "Off" || value === "Paused" ? "bg-ink/7 text-ink-muted" : "bg-teal/10 text-teal")}>{value}</button>}</div>;
}